import React from "react";

const Title = ({ title }) => {
   return (
      <div>
         <h1 className="text-5xl text-center text-cyan-900 mt-8 font-bold ">{title}</h1>
      </div>
   );
};

export default Title;
